- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.antigen.zsh: antigen.zsh
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules...]
